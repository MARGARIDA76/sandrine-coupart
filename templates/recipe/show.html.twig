{% extends 'base.html.twig' %}

{% block title %}Recipe{% endblock %}

{% block body %}
    <h1>Recipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ recipe.Titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recipe.Description }}</td>
            </tr>
            <tr>
                <th>Preparation</th>
                <td>{{ recipe.Preparation }}</td>
            </tr>
            <tr>
                <th>Repos</th>
                <td>{{ recipe.Repos }}</td>
            </tr>
            <tr>
                <th>Cuisson</th>
                <td>{{ recipe.Cuisson }}</td>
            </tr>
            <tr>
                <th>Ingredients</th>
                <td>{{ recipe.Ingredients }}</td>
            </tr>
            <tr>
                <th>Etapes</th>
                <td>{{ recipe.Etapes }}</td>
            </tr>
            <tr>
                <th>Allergenes</th>
                <td>{{ recipe.Allergenes }}</td>
            </tr>
            <tr>
                <th>Regimes</th>
                <td>{{ recipe.Regimes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recipe_index') }}">back to list</a>

    <a href="{{ path('app_recipe_edit', {'id': recipe.id}) }}">edit</a>

    {{ include('recipe/_delete_form.html.twig') }}
{% endblock %}
