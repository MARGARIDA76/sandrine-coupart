{% extends 'base.html.twig' %}

{% block title %}Recipe{% endblock %}

{% block body %}
    <h1>Recipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ recipe.Titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recipe.Description }}</td>
            </tr>
            <tr>
                <th>Tempts_de_Preparation</th>
                <td>{{ recipe.TemptsDePreparation }}</td>
            </tr>
            <tr>
                <th>Temps_de_Repos</th>
                <td>{{ recipe.TempsDeRepos }}</td>
            </tr>
            <tr>
                <th>Temps_de_Cuisson</th>
                <td>{{ recipe.TempsDeCuisson }}</td>
            </tr>
            <tr>
                <th>Igredients</th>
                <td>{{ recipe.Igredients }}</td>
            </tr>
            <tr>
                <th>Etapes</th>
                <td>{{ recipe.Etapes }}</td>
            </tr>
            <tr>
                <th>Allergenes</th>
                <td>{{ recipe.Allergenes }}</td>
            </tr>
            <tr>
                <th>Regime</th>
                <td>{{ recipe.Regime }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recipe_index') }}">back to list</a>

    <a href="{{ path('app_recipe_edit', {'id': recipe.id}) }}">edit</a>

    {{ include('recipe/_delete_form.html.twig') }}
{% endblock %}
